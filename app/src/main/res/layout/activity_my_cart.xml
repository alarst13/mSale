<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.MyCartActivity">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/cart_recycler_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"></android.support.v7.widget.RecyclerView>
        <ImageView
            android:id="@+id/cart_check_img_btn"
            android:clickable="true"
            android:layout_marginTop="10dp"
            android:layout_marginRight="10dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/cart_recycler_view"
            android:layout_alignParentRight="true"
            android:scaleType="fitXY"
            android:layout_alignParentBottom="true"
            android:background="@drawable/ic_check_mark"
            />
        <ImageView
            android:id="@+id/cart_cross_ims_btn"
            android:clickable="true"
            android:layout_marginTop="10dp"
            android:layout_marginRight="10dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/cart_recycler_view"
            android:scaleType="fitXY"
            android:layout_toLeftOf="@id/cart_check_img_btn"
            android:layout_alignParentBottom="true"
            android:background="@drawable/ic_x_mark"
            />
    </RelativeLayout>
</ScrollView>